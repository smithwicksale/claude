new task: clear todos/tasks/planning; perform a code review on current branch {branch-name} using ~/.claude/CODE_REVIEW.md; write a new report to ~/tmp/{dev-username}/code-review-{dev-username}-{branch-name}-{timestamp}.md; previous reviews may exist at ~/tmp/{dev-username}/code-review-{dev-username}-{branch-name}-*.md; do not update existing reports; HARD RULE: if any code change lacks new or updated tests, it MUST be reported as a 🔴 BLOCKER (B-*), NEVER Low or Medium, and the final verdict MUST be "❌ REQUEST CHANGES"
